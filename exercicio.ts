const multiplica = (numeroA: number, numeroB: number):number => {
    return numeroA * numeroB;
};

const saudacao = (nome: string): string => {
    return `Olá, ${nome}`;
};

console.log(multiplica(5, 6));
console.log(saudacao('Robert'));